{% extends 'base.html.twig' %}
{% block title %}New Usuario{% endblock %}
{% block body %}
<body id="fondo-crear-editar-forms">
<div class="container-fluid mt-1">
    <div class="row justify-content-center mt-1 row-forms-crear">
        <div class="col-5 mt-5">
            <div class="card-blanca">
                <div class="card-header text-center texto-no-torneos">
                    CREAR USUARIO
                </div>
                <div class="card-body">
                    {{ include('usuario/_form.html.twig') }}
                    <div class="col-12 justify-content-center d-flex">
                        <a href="{{ path('usuario_index') }}"></a>
                        <a class="btn btn-danger justify-content-center" href="{{ path('usuario_index') }}"> <i class="fas fa-arrow-circle-left"></i> Volver atrás</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="{{ asset('/js/Validations_js/RegisterValidate.js') }}"></script>
</body>
{% endblock %}
