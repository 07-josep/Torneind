{% block body %}
<body>
<form method="post" action="{{ path('torneo_delete', {'id': torneo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ torneo.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash-alt"></i> Eliminar definitivamente</button>
</form>
</body>
{% endblock %}
