{% extends 'base.html.twig' %}

{% block title %}Opinion{% endblock %}

{% block body %}
    <h1>Opinion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opinion.id }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ opinion.texto }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ opinion.fecha ? opinion.fecha|date('Y-m-d') : '' }}</td>
                <td>{{ opinion.fecha ? opinion.fecha|date('H:m')}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('opinion_index') }}">back to list</a>

    <a href="{{ path('opinion_edit', {'id': opinion.id}) }}">edit</a>

    {{ include('opinion/_delete_form.html.twig') }}
{% endblock %}
